# MCP

Model Context Protocol support is available in opt-in mode only.

- client and server are both disabled by default
- external network access is disabled by default
- sampling is disabled by default
- discovered tools require explicit approval before use
