# KG Providers

- `internal_sql`: supported default, no external dependency.
- `neo4j`: optional and blocked unless external providers are enabled.
- `falkordb`: optional and blocked unless external providers are enabled.
