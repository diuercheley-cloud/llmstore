name: agentic-production
description: Production-ready agentic environment with full auditing and isolation.
flags:
  AGENT_RUNTIME_ENABLED: true
  AGENT_CODE_INTERPRETER_ENABLED: true
  AGENT_CODE_SANDBOX_PROVIDER: gvisor
  AGENT_SANDBOX_ALLOW_SIMULATED_PROVIDER: false
  AGENT_SANDBOX_PRODUCTION_REQUIRES_ATTESTATION: true
  AGENT_READINESS_CHECKS_ENABLED: true
  AGENT_ENTERPRISE_OBSERVABILITY_ENABLED: true
  AGENT_PLUGIN_SUPPLY_CHAIN_ENABLED: true
  PLUGIN_SIGNATURE_REQUIRED: true
