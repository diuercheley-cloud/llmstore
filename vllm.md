# vLLM Backend Integration

## Overview
Added native integration with vLLM for high-throughput local inference.

## Features
- OpenAI compatible API mapping.
- Deep integration with routing layers.

## Governance
Controlled by `VLLM_BACKEND_ENABLED`. Default `false`. Users are free to use LM Studio or entirely offline CPU mode.
