docker:build:
  stage: build
  image: docker:latest
  services:
    - docker:dind
  script:
    - docker build -t llm-inference-stack/control-plane:latest -f docker/control-plane/Dockerfile .
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_TAG =~ /^v/
